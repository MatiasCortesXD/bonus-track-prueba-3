fase1:no se uso ia

fase2:
1. Crea src/components/EventoCard.jsx. 
2. El componente recibe por props: nombre, lugar, duracion, tipo, descripcion, fechas. 
3. Muestra la información de forma visual y ordenada. 
4. Aplica un estilo diferenciado según el tipo (un color/etiqueta distinto para Concierto, Teatro, Exposición). 

Buenas prácticas de desarrollo seguro obligatorias en este componente: 
• Define los tipos de las props con PropTypes (o documenta claramente cada prop) y entrega valores por defecto para evitar errores cuando falte un dato.
• Renderiza el texto siempre como contenido JSX normal. No uses dangerouslySetInnerHTML. 
• No dejes credenciales, claves ni datos sensibles escritos en el código.  

utilidad de prompt:

se edito :
fase3:1. Crea src/components/ListaEventos.jsx que reciba el array de eventos por props. 
2. Recorre el array con .map() y renderiza un EventoCard por cada evento · V3 Página 5 
3. Asigna correctamente la prop key usando el id de cada elemento (no uses el índice del array). 
4. En App.jsx, renderiza ListaEventos pasándole los datos importados

fase4: 
1. Crea src/components/FiltroTipo.jsx con botones o un selector para filtrar por el tipo: “Todos”, “Concierto”, “Teatro”, “Exposición”. 
2. En App.jsx, usa useState para guardar el filtro seleccionado y un campo de búsqueda por nombre. 
3. Aplica ambos filtros sobre el array antes de pasarlo a ListaEventos.
4. Usa renderizado condicional para mostrar el mensaje “No hay eventos que coincidan” cuando el resultado quede vacío.