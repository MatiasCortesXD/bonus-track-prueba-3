fase1:no se uso ia
fase2:

1. Crea src/components/EventoCard.jsx. 
2. El componente recibe por props: nombre, lugar, duracion, tipo, descripcion, fechas. 
3. Muestra la información de forma visual y ordenada. 
4. Aplica un estilo diferenciado según el tipo (un color/etiqueta distinto para Concierto, Teatro, Exposición). 

Buenas prácticas de desarrollo seguro obligatorias en este componente: 
• Define los tipos de las props con PropTypes (o documenta claramente cada prop) y entrega valores por defecto para evitar errores cuando falte un dato.
• Renderiza el texto siempre como contenido JSX normal. No uses dangerouslySetInnerHTML. 
• No dejes credenciales, claves ni datos sensibles escritos en el código.  

utilidad de prompt:

se edito :
fase3 : agrege el commit  “feat: renderizado de la lista con key”